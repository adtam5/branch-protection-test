{
	"name": "branch-protection-test",
	"version": "1.2.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"lint": "biome lint --no-errors-on-unmatched --write",
		"format": "biome format --no-errors-on-unmatched --write"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devEngines": {
		"packageManager": {
			"name": "pnpm",
			"version": "^11.21.0",
			"onFail": "download"
		}
	},
	"type": "module",
	"dependencies": {
		"@biomejs/biome": "^2.5.12"
	}
}
